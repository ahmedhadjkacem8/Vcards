server {
    listen 80;
    server_name cartevv.com www.cartevv.com;

    root /usr/share/nginx/html;

    location / {
        try_files $uri /index.html;
    }

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}
